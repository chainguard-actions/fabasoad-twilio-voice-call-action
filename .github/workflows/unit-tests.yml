---
name: Unit Tests

on: # yamllint disable-line rule:truthy
  pull_request:
    paths:
      - .github/workflows/unit-tests.yml
      - .tool-versions
      - jest.config.json
      - package.json
      - src/**
      - tsconfig.json
  push:
    branches:
      - main
  workflow_dispatch: {}

jobs:
  jest:
    name: Jest
    uses: fabasoad/reusable-workflows/.github/workflows/wf-js-unit-tests.yml@4e2279474e598bee3ae8ded28899a24bbc7bf971 # main
    permissions:
      contents: read
    secrets: inherit # pragma: allowlist secret
