---
name: Release

on: # yamllint disable-line rule:truthy
  push:
    tags:
      - "v*.*.*"

jobs:
  github:
    name: GitHub
    uses: fabasoad/reusable-workflows/.github/workflows/wf-github-release.yml@c5bd8945762dab6d2f5168b65f10355887ea40a3 # main
    permissions:
      contents: write
